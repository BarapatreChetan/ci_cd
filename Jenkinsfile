pipeline {
    agent any
    stages {
        stage("checkout Code") {
            steps {
                git url:'https://github.com/BarapatreChetan/ci_cd.git', branch:'main'
            }
        } 
    }
}
